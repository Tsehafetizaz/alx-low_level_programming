#include "main.h"

/**
* _strlen - returns the length of a string
* @s: string to be measured
*
* Return: length of the string
*/
int _strlen(char *s)
{
int lenth = 0;

while (*s != '\0')
{
lenth++;
s++;
}

return length;
}
